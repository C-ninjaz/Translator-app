## Placeholder for local prototyping — DO NOT commit your real key here.
## Replace the value below with your own RapidAPI key when running locally.
VITE_RAPIDAPI_KEY=YOUR_RAPIDAPI_KEY
# Optional: override host if you use a different RapidAPI translation API host
# Defaults to google-translator9.p.rapidapi.com
# VITE_RAPIDAPI_HOST=google-translator9.p.rapidapi.com

# Optional: override paths (provider defaults shown)
# VITE_RAPIDAPI_TRANSLATE_PATH=/v2
# VITE_RAPIDAPI_DETECT_PATH=/v2/detect
# VITE_RAPIDAPI_LANGUAGES_PATH=/v2/languages

# Proxy options (development overrides)
# Set to true to force using the local proxy during dev: VITE_USE_PROXY=true
# By default the client will use the proxy when built with mode=production.
# VITE_USE_PROXY=false
# VITE_PROXY_BASE=/api

# Server-side secrets (for Render/Netlify/Vercel) - DO NOT commit these to public repos
# RAPIDAPI_KEY=YOUR_RAPIDAPI_KEY
# Optional server tuning (rate limits and cache)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=60
# CACHE_TTL_MS=3600000
